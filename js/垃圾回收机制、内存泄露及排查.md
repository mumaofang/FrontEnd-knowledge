# 一、内存泄漏常见场景
- 意外的全局变量 -》 垃圾回收机制
- 未清理的定时器和监听器
- DOM 引用未释放 -》 dom的双重世界， jsV8, 渲染树树内存
- 闭包引用 -》 一个函数可以使用其他函数的作用域
- 缓存无限增长

# 二、 排查工具和方法
- chrome devTools 内存分析
- Performance 面板录制
# 三、排查步骤
- 步骤1：确认泄漏存在
- 步骤2：创建最小复现场景
- 步骤3：对比快照分析
- 步骤4：定位泄漏源

# 垃圾回收机制
GC 根原则：全局对象（window/globalThis）是垃圾回收的根，从根可达的对象永远不会被回收。
1. 垃圾回收的 "可达性" 概念、
// 垃圾回收的核心规则：
// 一个对象是否被回收，取决于它是否"可达"
// "根"（root）是永远不会被回收的起点
2. JavaScript 的 GC 根
// 在浏览器环境中，GC 根包括：
// 1. 全局对象 window (在浏览器中)
// 2. 当前执行栈中的变量
// 3. DOM 树的根节点
// 4. 其他内置的全局对象